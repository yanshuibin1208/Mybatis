CREATE TABLE author
(
    id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    sex CHAR(1) NOT NULL,
    birthday VARCHAR(11),
    address VARCHAR(100),
    phone VARCHAR(30)
);